---
title: 计算机视觉
date: 2024-01-01 00:00:00
permalink: /modules/03-computer-vision/
categories:
  - 学习模块
tags:
  - 计算机视觉
  - CNN
  - 目标检测
author:
  name: zimingttkx
  link: https://github.com/zimingttkx
---

# 计算机视觉

掌握卷积神经网络和计算机视觉的核心技术。

## 模块概览

| 属性 | 值 |
|:-----|:---|
| **前置要求** | 02-neural-networks |
| **学习时长** | 4-5 周 |
| **Notebooks** | 25+ |
| **难度** | ⭐⭐⭐ 中级到高级 |

## 学习目标

- ✅ 深入理解 CNN 的工作原理
- ✅ 掌握经典网络架构（VGG, ResNet, EfficientNet）
- ✅ 实现目标检测（YOLO, Faster R-CNN）
- ✅ 掌握图像分割技术（U-Net, Mask R-CNN）

## 子模块详解

### 01. CNN Basics | CNN 基础

| 主题 | 内容 |
|:-----|:-----|
| Convolution | 卷积操作原理 |
| Pooling | 池化层 |
| Feature Maps | 特征图可视化 |

**卷积操作：**

$$y[i,j] = \sum_{m}\sum_{n} x[i+m, j+n] \cdot k[m,n]$$

### 02. Classic Architectures | 经典架构

| 架构 | 年份 | 特点 | Top-5 Error |
|:-----|:----:|:-----|:-----------:|
| AlexNet | 2012 | ReLU, Dropout | 15.3% |
| VGG | 2014 | 3x3 卷积堆叠 | 7.3% |
| ResNet | 2015 | 残差连接 | 3.6% |
| EfficientNet | 2019 | 复合缩放 | 2.9% |

### 03. Object Detection | 目标检测

| 方法 | 类型 | 速度 | 精度 |
|:-----|:-----|:----:|:----:|
| YOLO | One-stage | 快 | 中 |
| SSD | One-stage | 快 | 中 |
| Faster R-CNN | Two-stage | 慢 | 高 |
| DETR | Transformer | 中 | 高 |

### 04. Image Segmentation | 图像分割

| 方法 | 任务 | 特点 |
|:-----|:-----|:-----|
| FCN | 语义分割 | 全卷积网络 |
| U-Net | 医学图像 | 编码器-解码器 |
| Mask R-CNN | 实例分割 | 检测+分割 |

## 代码示例

```python
import tensorflow as tf
from tensorflow.keras import layers, Model

# 简单 CNN 模型
def create_cnn():
    inputs = layers.Input(shape=(224, 224, 3))

    x = layers.Conv2D(32, 3, activation='relu')(inputs)
    x = layers.MaxPooling2D()(x)
    x = layers.Conv2D(64, 3, activation='relu')(x)
    x = layers.MaxPooling2D()(x)
    x = layers.Conv2D(128, 3, activation='relu')(x)
    x = layers.GlobalAveragePooling2D()(x)
    x = layers.Dense(256, activation='relu')(x)
    outputs = layers.Dense(10, activation='softmax')(x)

    return Model(inputs, outputs)
```

## 参考资料

- [Papers With Code - Computer Vision](https://paperswithcode.com/area/computer-vision)
- He, K., et al. (2016). Deep Residual Learning for Image Recognition. *CVPR*.
